[package]
name = "herdr-client"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
license = "Apache-2.0"
description = "Typed, synchronous client for the Herdr plugin socket API"

[dependencies]
interprocess = "2.4.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
